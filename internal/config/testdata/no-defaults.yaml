imap:
  host: imap.test.example.com
  username: test@example.com
  password: testpassword
  # port, mailbox, tls_mode omitted to exercise defaults

agents:
  - id: testagent
    token: "test-token-123"
    addresses:
      - "test+testagent@example.com"
